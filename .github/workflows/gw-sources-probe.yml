name: gw-sources-probe
on:
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Look up nine Games Workshop products against every keyless source
        run: python3 tools/gw-sources-probe.py
