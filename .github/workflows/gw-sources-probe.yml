name: gw-sources-probe
on:
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Second pass on what the first left open (slower, narrower)
        run: python3 tools/gw-sources-probe2.py
